# experimental —— 实验插件区

原型能力：跑在真实 PeriNest 内核上，但契约可变、无稳定性承诺。

- **默认不装**：不进 `PERINEST_Q_PLUGINS` 默认值
- **发布排除**：官方发布物不包含本区
- **稳定区禁止依赖**：内核与稳定插件不得 import 本区任何模块
- **不降质量标准**：本区插件照跑测试与 parity 对账——隔离的是稳定性承诺，
  不是质量要求（dsh 机械隔离模式）

候选：OA 审批流、进销存等业务插件将在此孵化，稳定后升入 `plugins/` 稳定区。
