/* PeriNest Antenna · 琥珀标本馆设计系统（与 Wing/Leg 同源）
   小程序无法用 webfont，衬线走系统栈：Songti SC / serif */

page {
  --amber: #b4560c;
  --amber-light: #d97706;
  --amber-glow: #f59e0b;
  --paper: #f5f1e8;
  --card: #fffdf7;
  --ink: #2b2118;
  --ink-soft: #8a7a68;
  --line: #e5dcc8;

  background: var(--paper);
  color: var(--ink);
  font-size: 28rpx;
}

/* 标本卡片（琥珀玻璃版）：不支持 backdrop-filter 的环境（部分安卓）自动回落实底 */
.specimen-card {
  position: relative;
  background: var(--card);
  border: 1px solid var(--line);
  border-radius: 24rpx;
  padding: 32rpx;
  margin: 24rpx;
  box-shadow: inset 0 2rpx 0 var(--glass-hi);
}
@supports (-webkit-backdrop-filter: blur(2rpx)) or (backdrop-filter: blur(2rpx)) {
  .specimen-card {
    background: var(--glass-bg);
    border-color: var(--glass-line);
    -webkit-backdrop-filter: blur(var(--glass-blur)) saturate(1.2);
    backdrop-filter: blur(var(--glass-blur)) saturate(1.2);
  }
}
.specimen-card::before {
  content: "";
  position: absolute;
  top: 16rpx;
  left: 16rpx;
  width: 10rpx;
  height: 10rpx;
  border-radius: 50%;
  border: 2rpx solid rgba(180, 86, 12, 0.5);
}

/* 衬线标本字：数字/英文 */
.font-specimen {
  font-family: "Songti SC", "Noto Serif SC", Georgia, serif;
}

/* 拉丁小标 */
.specimen-latin {
  font-family: Georgia, serif;
  font-size: 18rpx;
  letter-spacing: 6rpx;
  text-transform: uppercase;
  color: var(--ink-soft);
}

/* 琥珀玻璃表面：顶栏/浮层/容器通用；降级同 specimen-card */
.glass {
  background: var(--card);
  border: 1px solid var(--line);
}
@supports (-webkit-backdrop-filter: blur(2rpx)) or (backdrop-filter: blur(2rpx)) {
  .glass {
    background: var(--glass-bg);
    border-color: var(--glass-line);
    -webkit-backdrop-filter: blur(var(--glass-blur)) saturate(1.2);
    backdrop-filter: blur(var(--glass-blur)) saturate(1.2);
  }
}

/* 琥珀主按钮 */
.btn-amber {
  background: linear-gradient(160deg, var(--amber), var(--amber-light));
  color: #fff8ee;
  border-radius: 20rpx;
  text-align: center;
  padding: 24rpx 0;
  font-weight: 500;
  box-shadow: 0 12rpx 32rpx -12rpx rgba(180, 86, 12, 0.55);
}

/* 状态徽标 */
.badge-amber {
  border: 1px solid rgba(180, 86, 12, 0.4);
  color: var(--amber-light);
  border-radius: 999rpx;
  font-size: 22rpx;
  padding: 4rpx 20rpx;
}

/* 空态占位（Z-SH 模式）：图标 + 文案 + 可选动作按钮 */
.empty-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 120rpx 0;
}
.empty-icon { width: 160rpx; height: 160rpx; margin-bottom: 32rpx; opacity: 0.7; }
.empty-text { font-size: 26rpx; color: var(--ink-soft); margin-bottom: 40rpx; }
.empty-btn {
  background: linear-gradient(160deg, var(--amber), var(--amber-light));
  color: #fff8ee;
  border-radius: 999rpx;
  padding: 16rpx 56rpx;
  font-size: 26rpx;
  box-shadow: 0 12rpx 32rpx -12rpx rgba(180, 86, 12, 0.5);
}
