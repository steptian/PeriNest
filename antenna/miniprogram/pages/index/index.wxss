.title {
  font-size: 64rpx;
  font-weight: bold;
  margin: 12rpx 0 8rpx;
}
.slogan {
  font-size: 22rpx;
  font-style: italic;
  color: var(--ink-soft);
}
.section-title {
  font-size: 36rpx;
  font-weight: bold;
  margin: 16rpx 0 28rpx;
}
